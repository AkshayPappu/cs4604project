# Note to TA

The `sql/` folder includes our PostgreSQL data‐population script used to seed each table with sample records. For the actual relational schema (table definitions, constraints, and relationships), please refer to the `supabase/` folder where the live schema is maintained.

## App usage:
- need to add the club officer role and create a club in order to generate reports
